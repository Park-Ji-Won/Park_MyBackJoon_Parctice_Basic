A,B=map(int,input().split())
#input().split()
"""input() 함수가 한 줄 전체를 문자열로 읽어옵니다.

이어서 .split()을 호출하면, 기본적으로 
공백(스페이스·탭·개행) 기준으로 
그 문자열을 **여러 조각(리스트)**으로 나눕니다.

예를 들어 입력이 
3 5
라면, input().split()의 결과는 ["3", "5"]가 됩니다.
"""
#map(int,.....)
"""map 함수는 첫 번째 인자로 전달된 함수(int)를,

두 번째 인자로 전달된 반복 가능한 객체
(여기서는 리스트 ["3", "5"])의 각 요소에 차례로 적용하여

그 결과를 묶은 map 객체를 돌려줍니다.

즉 map(int, ["3", "5"])는 내부적으로 
int("3"), int("5")를 수행하여 
[3, 5]처럼 변환해 줍니다.
"""

#A,B =
"""
map(int, input().split())이 돌려주는 두 개의 정수(예: 3, 5)를

왼쪽의 변수 A, B에 차례로 할당(unpacking)합니다.

따라서 이 한 줄로 “첫 번째 입력값을 A에, 
두 번째 입력값을 B에” 담게 됩니다.
"""

print(A+B)